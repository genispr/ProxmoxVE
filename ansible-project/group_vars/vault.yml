# This file should be encrypted with ansible-vault in production
vault_admin_token: "replace_with_actual_token"
proxmox_api_password: "replace_with_actual_proxmox_password"
# ...additional secret variables as needed...
