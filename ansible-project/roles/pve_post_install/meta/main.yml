galaxy_info:
  role_name: post_pve_install
  author: Your Name / Organization Name  # Update with actual author details
  description: >
    Performs common post-installation tasks for Proxmox VE 8.x,
    including repository configuration, subscription nag removal, and system updates.
  license: MIT
  min_ansible_version: '2.13' # Updated to match current best practices

  platforms:
    - name: Debian
      versions:
        - bookworm # Proxmox VE 8.x is based on Debian Bookworm

  galaxy_tags:
    - proxmox
    - pve
    - virtualization
    - debian
    - system
    - configuration
    - security
    - updates

dependencies: []  # No external role dependencies
